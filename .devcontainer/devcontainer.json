{
	"name": "Nav2 Workspace",
	"dockerComposeFile": ["compose.yml"],
	"service":"ros2",

	"shutdownAction": "stopCompose",
	"workspaceFolder": "/root/ws/src",

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-vscode.cpptools-extension-pack",
				"eamodio.gitlens",
				"ms-iot.vscode-ros",
				"ms-azuretools.vscode-docker"
			]
		}
	},
	"postCreateCommand": "cd /root/ws/ && rosdep install --from-paths src --ignore-src --rosdistro $ROS_DISTRO -y && colcon build --symlink-install && echo 'source /root/ws/install/setup.bash' >> ~/.bashrc"
}