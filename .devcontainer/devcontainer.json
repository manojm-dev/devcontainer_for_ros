{
	"name": "Nav2 Workspace",
	"dockerComposeFile": ["compose.yml"],
	"service":"ros2",

	"shutdownAction": "stopCompose",
	"workspaceFolder": "/root/ws/",

    "forwardPorts": [],

	"customizations": {
		"vscode": {
			"extensions": [
				"ms-vscode.cpptools-extension-pack",
				"eamodio.gitlens",
				"ms-iot.vscode-ros",
				"ms-azuretools.vscode-docker"
			]
		}
	},
	"postCreateCommand": "rosdep install --from-paths src --ignore-src --rosdistro humble -y && colcon build && echo 'source /root/ws/install/setup.bash' >> ~/.bashrc"
}