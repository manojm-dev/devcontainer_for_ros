services:
  ros2:
    extends:
      file: ../common/common-compose.yml
      service: ros2-common

    build:
      context: .
      dockerfile: ../common/Dockerfile
      target: base
      args:
        ROS_DISTRO: rolling
        BASE_IMAGE: osrf/ros:rolling-desktop-full